# Phase 7 — Quantitative empirical measurements

Status: implemented on `work`. Fixture only. Design: `docs/PHASE7_DESIGN.md`.

Requested `effect_size.mean_forward_return.v1` facts mapped from Phase 3 evidence.
Unrequested families (Wasserstein, IC, …) are refused.
Current view lives in analytics, not `ops.current_*`.

Forbidden: stats engines, grouping, predictions, ranking, ingest.
