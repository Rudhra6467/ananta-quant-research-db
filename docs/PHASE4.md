# Phase 4 — Feature observation engine

Status: implemented on `work`. Fixture only.

A request catalog drives RSI(12–17) observations. Rolling Wilder state updates on new bars. Unused periods are refused.

Forbidden: RSI(2..50) materialization, combination cubes, exchange ingest, ranking, paper, predictions.
