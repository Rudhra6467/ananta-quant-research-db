# N3 — Real-tape laboratory (DESIGN ONLY)

Status: **DESIGN ONLY.** Execution forbidden until `snap-cryptolab10-kraken-1h-v1` has `complete=1` after authorized N2.6 load + audit.

Reuse Gate B `research_db.lab.engine.Laboratory`. Do not build a second research framework.
Success = P0–P20 survives real CRYPTO_LAB_10 tape. Not profitability.

Missing interval until official Q2/Q3 2026 OHLCVT: `2026-04-01Z → 2026-08-05T16:00Z`.

## Preconditions
1. Freeze `0871b49` meaning unchanged.
2. Snapshot `snap-cryptolab10-kraken-1h-v1`.
3. `complete=1` for `2021-09-01T00:00Z` → last complete hour.
4. Separate written N3 execution grant.
5. Live path still cannot scan raw/history.

## Inputs
Ten CRYPTO_LAB_10 Kraken spot 1h instruments. Requested RSI 12–17 only. RET(1), RANGE_VOL(1). Existing P5/P10 state/regime. Gate B lab engine.

## Experiments
`EXP_N3_PIPELINE_SURVIVAL@v1` requested features without cube.
`EXP_N3_PIT@v1` as_of hides later knowledge.
`EXP_N3_COHORT@v1` instrument/group isolation.
`EXP_N3_REGIME_REPRO@v1` versioned regimes reproducible.
`EXP_N3_GAPS@v1` gaps classified, not filled.
`EXP_N3_NEGATIVE@v1` insufficient/inconclusive retained.
`EXP_N3_RERUN@v1` new run id, same input_digest.

Cohorts: per instrument + `COHORT_G_CRYPTO_MAJORS` via P8 members_as_of.

## Acceptance (later execution)
Fixture suite green. No RSI cube. PIT deny. Rerun digest match. Inconclusive persists. AVAX listing gap remains. Report has no edge claim.

## Failure
Incomplete snapshot. Invented bars. Ranking/Agent/paper from N3. Constitution conflict.

N4–N9 are not opened by this brief.
