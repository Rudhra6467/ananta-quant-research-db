# Direction lock — 2026-09-04

This file is the project direction from this point. It does not add Phase 21+.
It does not authorize N3 execution.

Architecture freeze remains `0871b49`.
Snapshot `snap-cryptolab10-kraken-1h-v1` remains `complete=0`.
Gap remains `2026-04-01T00:00Z → 2026-08-05T16:00Z` until official Q2/Q3 2026 OHLCVT exist and a written load grant is issued.

## Corrections to the two review notes
1. N3 is **not** acquisition. Acquisition is N1–N2.6. N3 is the real-tape laboratory **after** `complete=1`.
2. N4 does **not** have to finish before the Agent *interface* can be exercised on complete real tape.
3. N4 **does** have to finish before the Agent may treat shift/event/analogue statements as researched claims.
4. Selective materialization is already the constitution (requested-only). Do not implement a CRYPTO_FULL optimizer before N3 produces workload evidence.
5. Fixture P11–P20 is architecture proof, not scientific validation.

## Immediate command
`INGESTION_ENABLED=false`. N3/N4/N5 execution flags stay false. N2.6 watch only. No substitute tape. No detector, Agent runtime, or paper venue.

When official zips appear:
official files → written **load** grant → load + snapshot audit → `complete=1` → written **N3 execution** grant → N3 laboratory.
Two separate grants.

## Activation ladder
load+audit → N3 laboratory → N5a catalog interface → N4 shift research → N5b Agent may cite N4 → N6 paper $0 → N7 lab acceptance → N8 CRYPTO_FULL then US/CA/IN → N9 trust ladder.

N5a may follow accepted N3 while N4 is open. N5b may not.

## Scaling law (reserved)
Permanent truth (never delete canonical bars/provenance).
Materialized research knowledge (requested or promoted).
Recomputable analytics (cheap deterministic functions stay computations until evidence says otherwise).
Learn economics on CRYPTO_LAB_10 before CRYPTO_FULL.
